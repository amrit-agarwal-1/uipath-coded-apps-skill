# uipath-coded-apps skill

The `uipath-coded-apps` skill (build & deploy UiPath Coded Web/Action Apps), extracted
from [UiPath/skills](https://github.com/UiPath/skills) (MIT). `AGENTS.md` is the entry
point — AI builders like Bolt read it automatically. Deep references live in `references/`.

Open in Bolt: `https://bolt.new/github.com/<owner>/<repo>`
